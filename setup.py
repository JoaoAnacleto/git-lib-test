from setuptools import setup, find_packages

setup(
    name='meu_modulo',  # Nome do pacote
    version='0.1',  # Versão inicial
    packages=find_packages(),  # Procurar pacotes dentro do diretório
    install_requires=[],  # Dependências adicionais, se houver
)